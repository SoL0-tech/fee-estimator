{
  "name": "valr-assessment",
  "version": "1.0.0",
  "description": "A workable fee estimator API based on the ETH blockchain. Goal is to predict which fee should be used when sending transactions",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jared Murphy",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.7.18",
    "ts-node": "^10.9.1",
    "tsc": "^2.0.4"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
